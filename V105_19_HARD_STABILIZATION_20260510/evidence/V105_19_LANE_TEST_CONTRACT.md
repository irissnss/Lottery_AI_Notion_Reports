# V105.19 Lane Test Contract

Main `/du-doan-test` output requires exact 20/20 contributors. Below budget is `PREVIEW_BELOW_BUDGET`, never `READY`. CONTROL rows are labeled `CONTROL`; anti-clone prefers a full-budget row differing from official when available, but independent consensus may match official.

Endpoints: `/api/admin/test-lane-history`, `/api/admin/test-lane-metrics`, `/api/admin/test-lane-readiness`, `/api/admin/test-lane-diff-vs-official`.
