# V106.05 Read This First

Key conclusion: MT has measurable source-rule relationships with MB low-cardinality prizes at D-1/D-2/D-3. The best global candidates are digit-position transforms from `MB G2#1` and `MB G1#1`; the strongest signals are scoped by weekday or MT station-set.

Do not promote directly from this backtest. Use `MT_MB_LOW_PRIZE_DIGIT_TRANSFORM_V1` as measurement-only shadow first.
