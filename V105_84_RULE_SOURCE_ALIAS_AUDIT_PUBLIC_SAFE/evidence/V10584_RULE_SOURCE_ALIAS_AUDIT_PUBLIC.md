# Rule Source Alias Audit

The audit distinguishes actionable rules from header-only rules. A header-only rule is not necessarily bad, but if the source station alias fails to match a known result row, it must not be treated as a fully actionable signal.
