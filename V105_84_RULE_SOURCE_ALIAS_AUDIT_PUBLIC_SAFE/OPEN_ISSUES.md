# Open Issues

- Decide whether to apply read-time alias normalization for `TP. TP. HCM` to `TP. HCM`.
- Keep Rule105 weight unchanged until trust/decay gates are reviewed.
- Continue treating empty-tail rules as non-actionable.
