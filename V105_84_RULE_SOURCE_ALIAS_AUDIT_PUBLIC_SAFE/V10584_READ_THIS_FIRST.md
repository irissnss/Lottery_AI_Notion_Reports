# V105.84 Read This First

Public-safe report. It contains no raw database, no trace log, no credentials, no server path, and no private operational identifiers.

Key finding: some rule source station names are not normalized, especially `TP. TP. HCM` vs `TP. HCM`, so those rules can show a header without candidate tails.
