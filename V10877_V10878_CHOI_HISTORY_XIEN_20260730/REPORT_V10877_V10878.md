# V10877 + V10878 — Lịch sử `/choi` và tình trạng xiên / 3 càng

**Ngày:** 30/07/2026

---

## V10877 — Lịch sử `/choi` hiển thị sai dữ liệu

> Owner: *"anh không thấy lịch sử sử dụng đoán đâu cả nha em, MT có thấy loáng thoáng nhưng có vẻ tối và không đầy đủ nha em xem xử lý dùm anh"*

**Nguyên nhân:** panel lịch sử gọi `/api/predictions` — endpoint này liệt kê dự đoán **theo từng model**, không phải số mà `/choi` thật sự đã chốt. Mười bốn dòng chia cho ba miền còn bốn năm dòng rời rạc mỗi miền, và không dòng nào là số đã chơi.

Hai lỗi hiển thị đi kèm, khớp đúng từng chữ owner tả: panel bị khoá `max-height:260px` nên bảng cụt, và thiếu class `.ch-panel-note` nên dòng chú thích không có style, chữ tối trên nền tối.

**Đã sửa:** thêm `_v10877_choi_history.py` + `GET /api/admin/choi-history` đọc từ `v10861_choi_display_output` (đúng bảng ghi số đã chốt). Panel mở sẵn, nới `max-height`, bổ sung style cho chú thích. Lịch sử giờ hiện số đã chốt, số nháy, số đài, và lãi lỗ ở cả hai lớp kế toán.

---

## V10878 — Xiên 2, xiên 3, 3 càng: đã dự đoán và đã chấm, nhưng `/choi` không hề dùng

> Owner: *"Cần xem lại phương án xiên 2 - xiên 3 và 3 càng dùm anh ở /choi nha em có vẻ chưa thiết kế luôn đó em?"*

**Cảm nhận của owner đúng một nửa.**

Đã tồn tại và hệ đã tự chấm từ lâu:

| Thành phần | Hiện trạng |
|---|---|
| `final_bundles.xien2` | 87/87 bundle tháng 7 |
| `final_bundles.xien3` | 82/87 |
| Lane `{REGION}_XIEN_V1` | chạy từ 04/06, 56 dòng mỗi miền, xuất 4 đuôi dùng cho xiên 2/3/4 |
| `xien2_status`, `xien3_status`, `lo3_status` | đều có giá trị đủ 177/177 |

**Phát hiện thêm: `lo3` chính là 3 càng.** Kiểm 177/177 dòng đều kết thúc bằng đúng bạch thủ của ngày đó — tức nó là bạch thủ cộng thêm một chữ số đầu. Ví dụ 29/07 MB `lo3=900` với `BT=00`; MN `lo3=696` với `BT=96`.

**Chỗ thật sự thiếu:** `v10861_choi_display_output` và `money_board_daily_lock` chỉ có trường `songthu` / `display_numbers`. Không có chỗ nào chứa xiên hay 3 càng, nên `/choi` không có gì để hiện dù dữ liệu đã nằm sẵn trong database.

**Chưa làm gì thêm** vì còn thiếu hai con số chỉ owner mới có: **vốn** và **tỷ lệ ăn** của xiên 2, xiên 3, 3 càng. Không có hai số này thì không tính được điểm hoà vốn, mà đưa lên `/choi` một thứ chưa biết lời lỗ thì đúng bằng việc đoán mò. Owner đã xác nhận sẽ cập nhật sau.
