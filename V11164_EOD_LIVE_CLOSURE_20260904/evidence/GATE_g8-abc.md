# gate8 · tang=EVIDENCE_COMPLETE · 9 phat hien

## TOM TAT

Sau cả ngày live 04/09, cột `output_counterfactual_rank` vẫn 0/17.121 — writer chạy 3 lần hôm nay (MN 16:39 · MT 17:30 · MB 18:31), mỗi lần đều CHẠM cột và bind hằng `None` vô điều kiện ở cả hai câu INSERT, nên 81 dòng mới đều NULL. Quét lại toàn cây 5.056 tệp cho 253 dòng khớp: không một reader nào của cột xuất hiện sau V11163 — 7 dòng trong mã sống đều là DDL hoặc danh sách cột, main.py và panel đều không đọc. Rủi ro của phương án A nay đã được chứng minh bằng tiền lệ thật chứ không còn là giả định: 17.001/17.121 dòng mang `created_at = 03/09`, tức V11158 đã viết lại gần trọn 6 tháng lịch sử trong một lần, và với `UNIQUE(date,region,ai_model)` + `INSERT OR REPLACE` thì mọi giá trị đổ vào trước đó đã bị xoá sạch. Đo lại trên DB production (chỉ đọc) tái lập ĐÚNG 2.538/2.094/444 của diễn tập V11163, nhưng NULL sau migration mang ÍT NHẤT BA nghĩa chứ không phải hai như đã công bố (thiếu lớp 135 dòng `parse_ok=0`), và câu «writer chạy mỗi giờ 16:00·17:00·18:00·20:00» trong `DE_XUAT_MATERIALIZATION_V11163.md` là SAI — phải rút lại theo PRJ-RETRACTION-001. Live 04/09 có tạo một yêu cầu mới thật (phép đo tiến cứu MT mở cửa sổ đúng hôm nay, nhưng artifact PHA 1 dừng ở 03/09 và không có job sinh hằng ngày), song yêu cầu đó nói về SINH KỊP THỜI trước kết quả chứ không trỏ tới cột này, nên chưa đủ biện minh cho C. Kết quả gate: `READY_FOR_OWNER_DECISION` — production 0 mutation, không chọn thay Owner.

## TRA LOI CAU HOI

**1 · Sau V11163 có reader THẬT nào xuất hiện không?** KHÔNG. Quét lại toàn cây (5.056 tệp, bỏ .git/venv/node_modules/__pycache__): 253 dòng khớp, trong đó 101 là artifact audit cũ, phần còn lại là backup/script. Mã SỐNG chỉ có 7 dòng ở 3 tệp: `_materialize_shadow_promotion_scorecard.py:44,408,500` · `_v11158_mat_thu.py:44,408,500` · `database.py:2278` — TẤT CẢ là DDL hoặc danh sách cột trong INSERT. `main.py` 0 dòng khớp (không route API). 0 view · 0 trigger · 0 index tham chiếu. `monitoring.html` có 1 dòng chứa chữ "counterfactual" nhưng là "M2 ROSTER A/B — chỉ counterfactual có context khớp lane mới được chấm", việc khác (RM-09: đọc ngữ cảnh, không đếm chuỗi thô). Không DB nào khác trên VPS chứa bảng này.

**2 · Có consumer/query/panel nào bị chặn vì cột rỗng không?** KHÔNG. Sáu tệp đọc BẢNG (`_v11102_bang_cong_don` · `_v11102_kiem_thuoc_model` · `_v11102_don_dong_ro` · `_v11155_vai_tro_theo_thoi_diem` · `_v10644_shadow_scoreboard` · `_materialize_multi_lane_shadow_p0`) không tệp nào chứa tên cột — chúng đọc `promotion_bucket`/`parse_ok`/`reliability_status`/`main_hit`. Cột này là cột DUY NHẤT trong họ của nó không có giá trị, và cũng là cột duy nhất không ai đọc: bốn cột láng giềng (`would_flip_baseline_to_win/lose`, `false_promotion_risk`, `bt_like_win`) đều 17.121/17.121 có giá trị.

**3 · Writer VẪN unconditional None hay đã thay đổi?** VẪN NGUYÊN, không đổi. Đọc mã thật (`sha256_16 = 291e45203ddfb501`, KHỚP GATE 0). Nhánh 1 (có dự đoán), dòng 420: `int(secondary_only), int(lo2_useful), int(main_hit), None,` → vị trí 17/34 chính là cột này. Nhánh 2 (thiếu dòng), dòng 520: `None, None, 0, 0, 0, 0, 0, None, 0, 0,` → `None` thứ ba là cột này. Đếm lại tham số 5+4+10+5+6+4 = 34 = số dấu `?`. Điểm quan trọng về ngữ nghĩa: writer KHÔNG PHẢI "không chạm cột" — nó CHẠM cột trong mọi INSERT và ghi đè NULL, nên bất kỳ giá trị nào đổ vào sẽ bị xoá ở lần materialize kế tiếp của chính bộ khoá đó.

**4 · Có cách biểu diễn NOT_COMPUTED khác COMPUTED_OUTSIDE_TOP10 không?** CÓ, nhưng KHÔNG làm được bằng một cột INTEGER đơn. Bốn đề xuất cụ thể (chỉ đề xuất, KHÔNG thực hiện): ① thêm cột `output_counterfactual_rank_status TEXT NOT NULL DEFAULT 'NOT_COMPUTED'` với từ vựng `COMPUTED_IN_TOP10 · COMPUTED_OUTSIDE_TOP10 · NOT_COMPUTABLE_NO_NUMBER · NOT_COMPUTED_OUT_OF_WINDOW` — kho đã có tiền lệ từ vựng trạng thái (`promotion_bucket` 5 giá trị, `reliability_status` 13 giá trị); ② sentinel `0` = ngoài top-10, NULL = chưa tính — KHÔNG khuyến nghị, đó là mã hoá ngầm, đúng họ lỗi V11158 (`MISSING_SHADOW_ROW` gộp 4 lớp, đẻ ra 1.058 lượt thua ảo); ③ bảng riêng đặc tả đầy đủ (chính là C), lưu được hạng > 10; ④ giữ ngoài DB nhưng sinh lại PHA 1 có `ranked` ĐẦY ĐỦ + trường trạng thái trong artifact. **Chặn cứng chưa ai nêu:** artifact PHA 1 chỉ lưu `top10` (`_run__v11159_pha1.py:122`), đo được 94/99 ô có hơn 10 ứng viên, tổng **420 hạng bị cắt cụt** — nên với dữ liệu đang có, "hạng 11" và "không có trong danh sách" KHÔNG phân biệt được. Mọi đề xuất có cột trạng thái đều cần SINH LẠI PHA 1 trước.

**5 · Live 04/09 có tạo yêu cầu MỚI đủ biện minh cho C không?** CÓ một yêu cầu mới thật, nhưng KHÔNG đủ. Phép đo tiến cứu MT (`docs/DANG_KY_TRUOC_MT_SHADOW_V11161.md`) mở cửa sổ sơ cấp ĐÚNG ngày 04/09, thước thứ cấp cần "hạng của ứng viên trúng đầu tiên" — tức cần bảng xếp hạng sinh TRƯỚC kết quả, mỗi ngày. Đo được: artifact PHA 1 dừng ở 03/09, **không có ô nào cho 04/09**, không cron/job nào sinh PHA 1 hằng ngày; 81 dòng scorecard sinh hôm nay (79 có số hợp lệ) rơi trọn vào nhóm "chưa tính" ⇒ nhóm NULL nhập nhằng tăng ~81 dòng/ngày. Ba lý do vẫn chưa đủ biện minh cho C: (a) yêu cầu nói về SINH KỊP THỜI, không phải nơi lưu là SQL hay JSON; (b) thước MT là hạng của MỘT SỐ trong bảng TOTAL — KHÁC ngữ nghĩa cột này (hạng output/candidate của TỪNG MODEL), cấm gộp hai thước (RM-21); (c) dòng scorecard chỉ RA ĐỜI SAU khi có kết quả (created_at MN 16:39:42 · MT 17:30:05 · MB 18:31:35, đều sau giờ xổ), nên một hạng sinh trước kết quả không có chỗ ghi tại thời điểm nó sinh ra — đó là lý do CẤU TRÚC vì sao cột trong bảng này không phải chỗ đúng.

**6 · Không reader + semantics chưa giải quyết ⇒ B còn là lựa chọn rủi ro thấp nhất không?** CÒN — B vẫn thấp nhất (0 đổi schema, 0 đụng writer, 0 dòng production bị ghi), và nguyên liệu còn nguyên: artifact `v11159_pha1_rank_dong_bang.json` (394.191 byte, sha256 `fe9a2741b872480b…`, băm nội bộ `9474b7bc…`) giữ `A.top10` cho cả 99 ô, suy ra giá trị trong 0,04s. NHƯNG phải sửa cách mô tả B: nó KHÔNG phải "không cần làm gì". Ba lỗ hổng đo được hôm nay: ① artifact dừng ở 03/09, không tự kéo dài; ② chỉ lưu top-10 ⇒ 420 hạng cắt cụt; ③ trường mang đúng tên `hang_cua_no_trong_A` trong chính artifact là NULL **973/973 = 100%** — ai đọc artifact mà tưởng đó là rank sẽ kết luận sai; nguyên liệu thật là `A.top10`, không phải trường đó.

**Kiểm thêm — writer chạy bao nhiêu lần hôm nay, có chạm cột không?** 3 lần, KHÔNG phải hằng giờ. `scheduler_logs` (naive = UTC, +7 ra VN): id=280006 → VN 16:39:55 MN inserted=27 · id=280228 → VN 17:30:18 MT inserted=27 · id=280473 → VN 18:31:48 MB inserted=27. Neo độc lập: chỉ có ĐÚNG 3 giá trị `created_at` phân biệt trong ngày — nếu có lô ghi thứ tư thì `created_at` của MN đã bị đổi. CÓ chạm cột: 81/81 dòng đi qua INSERT có liệt kê cột và bind `None` ⇒ 81/81 NULL. Đối chiếu 17.121 dòng scorecard: cột vẫn 0/17.121.


## PHAT HIEN (tieu de)
  - [PROVEN_DEFECT] Cột KHÔNG THỂ giữ giá trị: writer bind None vô điều kiện + UNIQUE + INSERT OR REPLACE, và đã có tiền lệ xoá 17.001 dòng
  - [PROVEN_DEFECT] Số đã công bố SAI phải rút lại: «writer chạy mỗi giờ (16:00·17:00·18:00·20:00)»
  - [PROVEN_DEFECT] NULL sau migration mang ÍT NHẤT BA nghĩa, không phải hai như đã công bố
  - [PROVEN_DEFECT] Trường `hang_cua_no_trong_A` trong artifact PHA 1 là NULL 973/973 = 100% — luôn None theo CẤU TRÚC
  - [NO_ANOMALY_FOUND] Không một reader nào của cột xuất hiện sau V11163 — quét lại toàn cây, không tin kết quả cũ
  - [NO_ANOMALY_FOUND] Tái lập ĐÚNG số diễn tập V11163 trên DB production (chỉ đọc): 2.538 / 2.094 / 444
  - [EXPECTED_BEHAVIOR] Artifact PHA 1 chỉ lưu top-10 ⇒ 420 hạng bị cắt cụt, không phục hồi được nếu không sinh lại
  - [OPERATIONAL_IMPROVEMENT] Live 04/09 tạo một yêu cầu vận hành mới thật — nhưng nó đòi SINH KỊP THỜI, không đòi cột này
  - [OPERATIONAL_IMPROVEMENT] Hai bản sao BYTE-IDENTICAL của writer nằm cạnh nhau trong thư mục đang phục vụ

## CHUA TRA LOI DUOC

**1 · AI là người đọc thật của cột/bảng này — chỉ Owner trả lời được.** Cả ba phương án đều có `unlock_value = 0` hôm nay vì không có reader. Câu hỏi chặn quyết định không phải «đổ hay không đổ» mà là «câu hỏi nào cần trả lời bằng SQL trên dữ liệu này». Không có câu đó thì C là đúng bài nhưng vô ích, còn A chỉ đổi chỗ lưu. Ghi NOT PROVEN, không suy luận lấp chỗ trống.

**2 · Ngữ nghĩa đích ở mức grain nào — bốn khoá Owner đã chốt CHƯA giải quyết.** Owner đã chốt «là hạng của OUTPUT/CANDIDATE trong một lần chạy TOTAL giả định; KHÔNG phải model leaderboard; KHÔNG phải oracle; KHÔNG trả lời published FINAL». Bốn khoá đó loại trừ đúng nhưng không nói MỘT DÒNG lưu hạng của cái gì khi một model ra tới 2 số (`main_number` + `secondary_number`) và bản công bố có 5 họ output. Diễn tập V11163 tự chọn `main_number` — đó là một lựa chọn của agent, chưa được Owner xác nhận. Cấm tự nâng thành đã chốt.

**3 · Có nên lập lịch sinh PHA 1 hằng ngày cho phép đo tiến cứu MT không.** Đo được là hiện KHÔNG có job nào và artifact dừng ở 03/09; nhưng `MT_PREREGISTRATION` vẫn là `PROVISIONAL_AGENT_PROPOSED`, chưa `OWNER_LOCKED`. Mở một cron mới nằm ngoài phạm vi gate này (cấm mở FU/Plan mới), nên chỉ ghi nhận là việc cần Owner quyết, không tự làm.

**4 · Khi nào «cột trạng thái» cần bao nhiêu giá trị — chưa đo đủ.** Đã đo được 4 lớp (`COMPUTED_IN_TOP10` 2.094 · `COMPUTED_OUTSIDE_TOP10` 444 · `NOT_COMPUTABLE_NO_NUMBER` 135 · `NOT_COMPUTED_OUT_OF_WINDOW` 14.448), nhưng chưa kiểm liệu lớp thứ tư có cần tách tiếp thành «chưa tính vì model chưa ra đời» (`PRE_EXISTENCE_COVERAGE_GAP` 4.013 dòng toàn bảng) hay không — việc đó cần một phép đo riêng cắt chéo giữa cửa sổ artifact và mốc tồn tại model. INDETERMINATE.

**5 · Lineage 3-càng dùng chung hay tách khỏi quyết định này.** V11163 gộp hai thứ vào một đề xuất và cùng nghiêng về B. Phiên này chỉ đo phần `output_counterfactual_rank` theo đúng phạm vi Gate 8; phần lineage 3-càng KHÔNG đo lại, nên không phát biểu gì mới về nó. Ranh giới `MOC_THUAT_T