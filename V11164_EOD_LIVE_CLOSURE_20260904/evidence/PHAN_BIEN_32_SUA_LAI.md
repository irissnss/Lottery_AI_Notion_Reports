 1. DUNG_MOT_PHAN   Giữ nguyên tiêu đề, giữ nguyên nhãn PROVEN_DEFECT. Chỉ sửa phần «bằng chứng» thành: bằng chứng: main.py:10491 `'quality_filtered_models': sorted(filtered_models) if filtered_models else []` và main.py:10511 `'wr_gate_filtered': sorted(filtered_models) if filtered_models else []` — CÙNG một biểu thức, hai tên khác nhau · main.py:9840 cap V10752 (`_MAX_VOTERS_BY_REGION={"MT":13}`, khai báo 9823) đổ thẳng vào chính `fil

 2. DUNG_MOT_PHAN   Giữ nhãn PROVEN_DEFECT nhưng sửa tiêu đề và hệ quả (3), bổ sung phạm vi: TIÊU ĐỀ: «combo-super: dòng log của scheduler luôn in `models=0` vì đọc khoá trần `models_used` mà `run_combo_super()` chưa bao giờ trả — khiếm khuyết GHI VẾT, không chạm output.» HỆ QUẢ — viết lại: (1) [GIỮ] scheduler.py:3981 in `models=0` ở mọi lượt/miền. Tái lập: `scheduler_logs` regex `COMBO Super (\w+): .*?models=(\d+)` → {0: 505}, 0 dòng k

 3. DUNG_MOT_PHAN   SUA TIEU DE: "Cap V10752 (co y, owner duyet 25/06) bi ke toan chung mot tap voi model TRUOT GATE, nen bi doc nguoc thanh «bundle thieu model» — MT bi loai khoi do luong chinh 71 NGAY LIEN TIEP ke tu ngay cap co hieu luc". SUA NOI DUNG — ba cho: 1) THAY doan "Mau tinh rolling WR/TOP1 lech han theo mien: MN 81 ngay, MT 18 ngay, MB 17 ngay" bang: "`_get_combo_super_predictions(90)` ap `LIMIT days_back*3 = 270` SAU khi l

 4. DUNG_MOT_PHAN   TIÊU ĐỀ SỬA: "run_id của lane rerun ghi tiền tố miền của LƯỢT CHA thay vì miền của DÒNG — nhãn sai, chưa có hộ tiêu thụ nào bị hại" NỘI DUNG SỬA: Quy ước tiền tố = miền của DÒNG được ghi, chứng minh bằng code (không suy từ tên): scheduler.py:1352 `f"{region}_{today}_{uuid[:8]}"` · :5393 `f"{target_region}_..."` · **:5764 `_mb_rerun_run_id = f"MB_{date_str}_{uuid}"`** — lượt AI rerun MB tuy chạy lồng trong cascade sau

 5. DUNG            Giu nguyen ket luan va nhan PROVEN_DEFECT. Ba chinh sua ve DO CHINH XAC (khong doi con so nao): 1. THEM bang chung manh nhat con thieu: trich chu thich :6716-6718 "Ba truong duoi day bam CHINH chuoi sap gui di". Day la mau thuan TRUC TIEP giua y do thanh van va hanh vi — manh hon lap luan ti le phu, va dap dung yeu cau "PROVEN_DEFECT phai co bang chung truc tiep". 2. SUA cau "bon khoi nay duoc noi vao": dung ve MA nh

 6. DUNG            Giữ nguyên phát hiện và nhãn PROVEN_DEFECT. Sửa BA chỗ trong phần bằng chứng/diễn đạt: (1) SAI NGUỒN BẰNG CHỨNG — số `15448` KHÔNG có trong journal. Câu «đối lại 17 dòng `[CONTEXT_PACK] Injected 10977/11557/15448 chars`» gộp nhầm nguồn. Đo lại `artifacts/v11164_journal_0409.txt` (10.841 dòng): phân bố `Injected` là `{10977: 9, 14142: 12, 11557: 8, 14536: 9}` — tổng 38 dòng, KHÔNG có 15448 và KHÔNG có 18427, vì journa

 7. DUNG_MOT_PHAN   Tach phat hien lam HAI, moi cai mot nhan: === PHAT HIEN 1 (giu, sua tieu de) — EXPECTED_BEHAVIOR === Tieu de: "Regime prompt (CONTEXT_ONLY_V2 vs LEGACY) da dinh tuyen theo LUOT, khong con theo MODEL — 60/60 luot 04/09 nhat quan; suc phan biet thuc te n=3" Noi dung phai ghi du: - 60 luot LLM co trace 04/09: 27 LEGACY_PROMPT / 33 CONTEXT_ONLY_V2. Phan hoach theo MODEL sach: 11 model chi chay shadow_auto_eval -> 33/33 C

 8. DUNG_MOT_PHAN   Giữ nguyên kết luận, sửa ba chỗ trong phần bằng chứng và bổ sung một giới hạn nhãn: 1) BỎ `:3042` khỏi danh sách vị trí dấu. Viết đúng: «ba dấu rơi vào Phase 14A là #1 `HIỆU SUẤT THEO MODEL` (:3040), #2 `win_rate` (:3043), #4 `ưu tiên patterns từ models` (:3043). Dòng :3042 chứa chữ `win_rate` trong MÃ (khoá dict) nhưng render ra CON SỐ ⇒ đóng góp 0 dấu — kiểm: chạy riêng dòng :3042 qua `sum(1 for d in _dau_o_nhiem i

 9. DUNG_MOT_PHAN   GIỮ phát hiện và GIỮ nhãn PROVEN_DEFECT. Sửa ba chỗ: 1) Sửa hai con số (đếm hai lần dải phân cách 12 ký tự). Thay: «chuỗi thật ≥ 24.435 + 10.977 + 2 + 15.256 = 50.670 ký tự ⇒ trường báo THIẾU ≥ 26.235 ký tự, tức ≥ 51,8%» bằng: «chuỗi thật = 24.435 − 12 + 10.977 + 2 + 15.256 = 50.658 ký tự (12 = len("\n<<<USER>>>\n"), nằm trong runtime_prompt_chars nhưng KHÔNG thuộc chuỗi gửi đi) ⇒ trường báo THIẾU 26.223 ký tự, tức 5

10. DUNG_MOT_PHAN   Giu nguyen phat hien va nhan PROVEN_DEFECT. Sua hai cho truoc khi dua vao bao cao: 1. Thay «Quét ngược 30 ngày: 88/88 cặp (ngày, miền) đều lệch» bang: «Quét 30 ngày (2026-08-06 → 2026-09-04), lọc bỏ dòng shadow-lane: 86/86 cặp (ngày, miền) đo được đều lệch; 3 cặp không đủ dữ liệu đối chiếu. Mở sang 31 ngày: 89/89. Bảy model official không phân tán ở BẤT KỲ cặp nào (0/86).» — kem lenh tai lap: /root/Lottery_AI_Test/ar

11. DUNG_MOT_PHAN   GIỮ finding nhưng sửa ba chỗ: 1. TIÊU ĐỀ — bỏ chữ ám chỉ mất dấu vết: «smart-ensemble ghi NHÃN SAI cha mẹ trong `verdict_reason` và `analysis_text`: chọn top-2 ML động nhưng nhãn viết cứng «Meta / LSTM» (dấu vết thật vẫn còn ở `models_used`)» 2. XOÁ HẲN mệnh đề «và chính vì thế KHÔNG thể phát hiện trùng cha mẹ khi đọc `predictions`» — ĐÃ BỊ BÁC BỎ. Thay bằng: «Hậu quả: mọi người/mọi script đọc `verdict_reason` hoặc h

12. DUNG            Ba chỗ nên chỉnh khi ghi vào báo cáo: 1. SỬA SỐ ĐỐI CHỨNG (RM-11). Phát hiện viết «gpt-5.5 có shadow_auto_eval 15/15 ngày ở cả ba miền». Đo lại: **44 dòng, không phải 45** — MN 15 ngày · MB 15 ngày · **MT chỉ 14 ngày** (thiếu 28/08, cùng ngày gpt-oss-120b cũng thiếu MT ⇒ là lỗ hổng cả miền hôm đó, không riêng model). Viết đúng: «gpt-5.5 có shadow_auto_eval 44 lượt / 15 ngày (MN 15 · MB 15 · MT 14)». Truy vấn: `SELECT

13. DUNG_MOT_PHAN   Giu nguyen phan quan sat (da tai lap 100%), thay hai cau tran an va noi ro quy uoc: CAU CU: "Bay trong chin model thuoc lane shadow nen khong bo phieu vao bundle; top1 chi co 6 voter, entropy 3,164 va margin 0,0267 deu o muc nen, tuc herding nay CHUA truyen vao output cong bo." CAU MOI: "Bay trong chin model thuoc lane shadow nen khong bo phieu vao bundle. Nhung cum NAY DA CHAM VAO OUTPUT: bach thu cong bo cua bundle

14. DUNG            Sửa hai chỗ trong phát hiện, giữ nguyên nhãn PROVEN_DEFECT: 1. «`_v11083_sinh_dieu_huong.py` được dựng ngày 18/08» → sửa thành «dựng ngày 17/08/2026 (commit 43485fd, `git log --diff-filter=A -- web/backend/_v11083_sinh_dieu_huong.py`)». Mốc 17/08 mới khớp với chính docstring của nó (27/07 → 17/08 = đúng 21 ngày). 2. «RM-15: cổng phải nhớ gọi là cổng không tồn tại» → phát biểu chính xác hơn: «`_v11083` là BỘ SINH kèm 

15. DUNG_MOT_PHAN   1) ĐỔI NHÃN: PROVEN_DEFECT → **OPERATIONAL_IMPROVEMENT**. Lý do phải ghi kèm: «94» tái lập ĐÚNG BẰNG 94 tại commit tracker 709efaf (21/08 10:01) trên chính thước cửa sổ [−14,+21] mà tệp khai ở dòng 5; tệp tự đề ngày sinh ở dòng 5 và tự khai là máy sinh ở dòng 1; và grep toàn kho không thấy hook/cron/scheduler nào gọi _v11083_sinh_dieu_huong.py — nên KHÔNG có cơ chế nào chết, chỉ là bộ sinh chạy tay chưa được chạy lại

16. DUNG_MOT_PHAN   GIỮ cơ chế, SỬA số + cite + nhãn, TÁCH LÀM HAI phát hiện: **PH-1 (giữ `PROVEN_DEFECT`, thu hẹp phạm vi)** — *«Số đếm voter trong bundle KHÔNG de-dup huyết thống, và số đếm thô đó đi thẳng vào `consensus_level`»* - nội dung: `main.py:10368-10372` sinh `top1_reason` bằng `voters=len(number_voters[...])`; `main.py:10339-10345` lấy **cùng** số đếm ấy ra `consensus_level` (≥4 → `strong`). Không có bước gộp cha-con. - bằng

17. DUNG_MOT_PHAN   Giữ phát hiện, hạ phạm vi và sửa ba chỗ sai: TIÊU ĐỀ SỬA THÀNH: «Sổ rút lại đứng ở 7 mục từ 18/08 — cổng PRJ-RETRACTION-001 VẪN CHẠY MỖI GIT COMMIT nhưng DANH SÁCH CHẶN đã cũ 17 ngày, mù đúng 4 kết luận vừa bị bác» BỎ HẲN (sai sự thật): mệnh đề «cổng này cũng KHÔNG nối vào hook» và bằng chứng «grep '11085|rut_lai' .cursor/hooks.json .cursor/hooks/*.py → 0». Thay bằng ghi nhận đúng: cổng ĐƯỢC gọi qua `.claude/hooks/co

18. DUNG_MOT_PHAN   TÁCH LÀM HAI VÀ HẠ TẦNG: (A) «combo-super gọi lượt LLM riêng cho thành viên AI nó chọn» → `EXPECTED_BEHAVIOR` + `OPERATIONAL_IMPROVEMENT`. Viết lại nội dung: cơ chế đúng như mô tả và đã chứng minh bằng mã (`combo_super.py:1374 → :1134`), nhưng nó là thiết kế đã thành văn (docstring `:1238`, CLAUDE.md §59), không phải lỗi. Bỏ chữ «trả tiền hai lần» và «thay vì dùng dòng đã lưu» — thay bằng: «tốn thêm 26.621–31.137 tok

19. DUNG_MOT_PHAN   GIU: luan diem chinh ("khong model nao vuot nen co y nghia" tren cua so 06/07-03/09), cach do nen, bootstrap cum theo ngay, nhan SUSPICIOUS_NEEDS_MORE_EVIDENCE. SUA 1 — loc NO_ANSWER truoc khi do, dung dung bo loc production: Them dieu kien cua combo_super._ti_le_bach_thu (loc theo NOI DUNG, khong theo status, de dung ca voi dong lich su chua doi nhan): AND NOT (main_numbers IS NULL OR main_numbers = '' OR main_numbe

20. DUNG_MOT_PHAN   Sua tieu de va noi dung phat hien thanh: «No bao cao §57 khong giam ma TANG: 38/232 (do 02/09, commit bd0ea86) -> 40/240 (do 04/09), trong do 21 ban thieu han bao cao + 2 duong tinh gia cua cong + 17 ban truot khung.» Ba sua cu the: 1. TACH danh sach 23 thanh hai ro: · THIEU THAT (21): V11156 V11037C V11087B V11044B V11037B V11033B V11032B V11029 V11027 V11026 V11021B V11021 V11019 V10997 V10992 V10991B V10940 V10939

21. DUNG_MOT_PHAN   Giu nguyen tieu de va toan bo con so (30/30 hang · 81/81 trong so · nhan NO_ANOMALY_FOUND). Chi sua cau ket luan cuoi. THAY: "Dieu nay chung minh: cong thuc cham diem production tai lap duoc, khong co buoc an, va trong so production dung hom do KHONG chua du lieu sau ngay dich." BANG: "Dieu nay chung minh HAI menh de, ca hai deu duoc gioi han dung pham vi: (1) Cong thuc cham diem production TAI LAP DUOC tren ba bundl

22. DUNG_MOT_PHAN   GIỮ phần cơ chế, HẠ nhãn, BỎ câu nhân quả, ĐỔI trọng tâm sang consensus_level. TIÊU ĐỀ (thay): «combo-super gọi lại chính model đã bỏ phiếu ⇒ nhãn consensus_level bị thổi từ moderate lên strong; KHÔNG đổi bạch thủ ngày 04/09» NHÃN: `PROVEN_DEFECT` → tách làm hai: · Cơ chế gọi lại + tự bỏ phiếu = `EXPECTED_BEHAVIOR` (thiết kế đã ghi: CLAUDE.md §59 · combo_super.py:2-5 · model_registry.py:872) · Đếm voter thô cho conse

23. DUNG_MOT_PHAN   Tách làm HAI phát hiện, hạ tầng, sửa mệnh đề sai: 【F1 — EXPECTED_BEHAVIOR (đã biết · đã ghi · owner khoá · đang có 3 lane xử)】 "Bộ gộp smart-ensemble/smart-ml bỏ phiếu vào bundle CẠNH chính model cha của nó, không khử trùng lặp theo dòng dõi." - Cơ chế đúng: hai bộ gộp CHẠY LẠI model cha trong bộ nhớ (`_run_free_model_prediction`, scheduler.py:2855-2868 · :3181-3213) rồi xếp lại và bỏ phiếu như một model riêng — KHÔN

24. DUNG_MOT_PHAN   Sửa tiêu đề và phần tầng 5 như sau: TIÊU ĐỀ: «Tầng 1 = 2 = 3 = 4 không lệch (tái lập 3/3 miền, kể cả prefix lô-3); TẦNG 5 KHÔNG ĐO ĐƯỢC cho 04/09 — bề mặt công khai bị viewer-freeze kẹp ở 2026-06-07, không phải bị 401.» NỘI DUNG SỬA: 1. Bỏ câu «Tầng 5 `/api/final-bundle` … khách nhận HTTP 401 — không có output công khai để so» ở vai trò kết luận về tầng 5. Giữ nó ĐÚNG chỗ: đó là bằng chứng về một bề mặt ADMIN, không 

25. DUNG_MOT_PHAN   Giữ phát hiện nhưng viết lại thành hai tầng tách bạch, và bỏ mệnh đề đủ: TIÊU ĐỀ: "G7-F2 — _safe_stdio_ctx mutate sys.stdout TOÀN CỤC trong khi mỗi lượt model chạy ở luồng riêng ⇒ ĐUA khôi phục stream; lỗi TIỀM ẨN, chưa có biểu hiện trên sản xuất." NHÃN: PROVEN_DEFECT (tầng MÃ — có bằng chứng thực thi trực tiếp, tái lập 4/4) + PRODUCTION_MANIFESTATION = NOT PROVEN / INDETERMINATE (bắt buộc ghi kèm, RM-12). PHẢI THÊM 

26. DUNG_MOT_PHAN   Giữ nguyên phần cơ chế, RÚT LẠI câu tác động, HẠ NHÃN từ PROVEN_DEFECT → OPERATIONAL_IMPROVEMENT. Tiêu đề sửa: "G7-F3 — _ensure_safe_stdio chỉ lưu stream HỎNG (đúng thiết kế); nhánh stream gốc = None để lại _SafeNullWriter vĩnh viễn — lệch với bản _v10545, hiện KHÔNG khả đạt trong production" Nội dung sửa: - Câu hỏi 2 và 3 của cổng: saved[idx]=stream nằm trong `if unusable:` (scheduler.py:247-249) nên _ensure_safe_st

27. DUNG_MOT_PHAN   Giu phat hien nhung THU HEP pham vi, HA TANG, va SUA LAI co che. De nghi viet lai: TIEU DE: "G7-F1 (da sua) — Probe write('')+flush() mu voi PIPE mat dau doc; KHONG mu voi socket journald cua production" NOI DUNG DUNG: - Dieu kien unusable (scheduler.py:240-246) bo sot DUNG MOT lop: fd la PIPE con dau doc da chet. Ly do o kernel: Linux pipe_write thoat som "Null write succeeds" cho ghi do dai 0, truoc ca khi kiem pip

28. DUNG_MOT_PHAN   Giu phat hien nhung VIET LAI phan 272, bo chan straw-man: TIEU DE: G7-F4 — Hai docstring cua khoi safe-stdio mo ta khong dung pham vi thuc thi (scheduler.py:254, :272) NHAN: OPERATIONAL_IMPROVEMENT · lop: DOC_SAID ≠ CODE_DID (§62) · khong co hau qua hanh vi (1) scheduler.py:254 — GIU NGUYEN Y, noi ro cach doc: TRUOC: """Restore stdout/stderr after _ensure_safe_stdio() if originals were valid.""" THUC TE: _ensure_safe

29. DUNG_MOT_PHAN   Giữ phát hiện, sửa TIÊU ĐỀ và hai con số, tách rõ «cơ chế» với «thiệt hại»: TIÊU ĐỀ SỬA: «Cột `output_counterfactual_rank` KHÔNG THỂ giữ giá trị: writer bind hằng `None` vô điều kiện ở vị trí 17/34 trong CẢ HAI câu INSERT + `UNIQUE(date,region,ai_model)` + `INSERT OR REPLACE` + KHÔNG có guard skip — và bảng đã có tiền lệ ghi đè hàng loạt (31.934 dòng bị xoá trong đời bảng, 16.920 dòng lịch sử bị viết lại trong MỘT đợ

30. DUNG            Giu nguyen ket luan va nhan PROVEN_DEFECT. Ba diem nen sua cho CHINH XAC hon, khong diem nao doi ket luan: 1. BO SUNG hang rao manh hon vao bang chung. Phat hien chi neu cho_phep/them_nguon, nhung hang rao chan truoc va manh hon la SQL: `_v11161_rank_gen.py:102-103` (ban artifacts/_run__v11159_rank_gen.py:102-103) — `if not gom_shadow: sql += "AND COALESCE(run_source,'') != 'shadow_auto_eval'"`. Dong shadow bi loai n

31. DUNG            Giữ nguyên kết luận và nhãn PROVEN_DEFECT, sửa ba chỗ: ① CƠ CHẾ BỊ MÔ TẢ SAI (số không đổi). Phát hiện ghi «tập ghi của diễn tập lấy WHERE parse_ok=1 AND main_number IS NOT NULL». Mã thật lọc `WHERE parse_ok=1` theo TỪNG Ô của artifact PHA-1 rồi `if not mn: continue`. Đo được `parse_ok=1 nhưng không có số = 0` nên hai lối ra cùng 2.538 — nhưng yếu tố loại trừ THẬT trong cửa sổ là `parse_ok=0`, KHÔNG phải `main_number

32. DUNG            Giu ket luan PROVEN_DEFECT, nhung sua NOI DUNG ban rut lai cho du bon phan cua PRJ-RETRACTION-001: 1) CHO GOC — BA noi, khong phai hai: - CHANGELOG.md:14 (muc V11163, "② Writer hang gio se XOA SACH") - docs/DE_XUAT_MATERIALIZATION_V11163.md:86 (bang muc 5, o "rui ro va cham") - docs/FOLLOW_UP_TRACKER.md:25 (muc "② Writer hang gio se XOA SACH gia tri vua do") 2) NGUYEN VAN CAU SAI — chep rieng tung noi: - CHANGELOG.md